Zapytanie 1: SELECT `nazwa`,`ulica` FROM `kwiaciarnie` WHERE `miasto` LIKE "Malbork";
Zapytanie 2: SELECT `id_kwiaciarni`, COUNT(`id_zam`) FROM `zamowienia` GROUP BY `id_kwiaciarni`;
Zapytanie 3: SELECT kwiaciarnie.nazwa, kwiaciarnie.miasto, zamowienia.kwiaty FROM `kwiaciarnie` JOIN `zamowienia` ON kwiaciarnie.id_kwiaciarni = zamowienia.id_kwiaciarni WHERE zamowienia.data LIKE "2017-01-07";
Zapytanie 4: CREATE TABLE klienci (id int AUTO_INCREMENT PRIMARY KEY NOT NULL, imie varchar(15), nazwisko varchar(15), rabat int UNSIGNED);